export interface ISchool {
  id: number;
  name: string;
  address: string;
  email: string;
  phone: string;
  whatsapp: string;
  status: string;
  is_accept: boolean;
  city?: string;
  region?: string;
}
